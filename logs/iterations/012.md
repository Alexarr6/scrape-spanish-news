# Iteration 012

- Date (UTC): 2026-03-26 19:45
- Base branch: iter/011
- Working branch: iter/012
- Goal: simplify the lower Stories area by aligning Articles by source with the shared section shell and reducing selected article detail to only the useful product surface.
- Planner notes: Inspected `StoryFocusPanel.tsx` and found the final section still outside the shared major-section shell; selected article detail still includes editorial analysis, cluster membership diagnostics, and semantic context metrics. PLAN.md now approves a frontend-only pass that wraps Articles by source in the shared shell, keeps only header + summary + Open in Explorer + nearby articles in the drill-in, and removes the debug-style blocks from Stories.
- Architect notes: Approved the lower Stories simplification as a frontend-only IA pass. `Articles by source` should be rebuilt onto the same `story-focus-major-section` + `story-focus-major-shell` structure as the upper sections, with that shell switching between grouped source cards and the selected article drill-in. The drill-in is intentionally minimal: back affordance, source/section eyebrow, headline, published date, summary/excerpt fallback, `Open in Explorer`, and nearby articles when available. Remove `Open article ↗`, editorial analysis, cluster-membership diagnostics, semantic context metrics, and any dead helpers/imports tied only to those blocks.
- Implementer notes: Reworked the final Stories section so `Articles by source` now lives inside the shared major-section shell and uses a light inner-shell variant to avoid double-card nesting. Simplified `ArticleDetailSection` to the approved product-facing content only, moved nearby articles into the selected article drill-in, and deleted the article-level diagnostic/analysis helpers that no longer belong in Stories.
- Verification: Ran `cd frontend && npm run build` successfully. Build completed with existing non-blocking warnings about `spawn` in `@loaders.gl/worker-utils` and large chunks.
- Result: iter/012 ships a bounded frontend cleanup: the lower Stories area is structurally aligned with the rest of the page, and selected article detail is now a clean browse-and-jump surface instead of a half-debug pane.
