# Iteration 024

- Date (UTC): 2026-03-26 22:48
- Base branch: iter/023
- Working branch: iter/024
- Goal: remove the legacy scrape-only scheduler family while preserving the canonical modern Stories / Explorer refresh surface
- Planner notes:
  - Confirmed the bounded legacy removal set is exactly: `scripts/run_scheduled.sh`; Makefile variable `SCHEDULER_SCRIPT`; Makefile targets `scheduler-dry-run`, `scheduler-once`, `status`, `tail-log`; the legacy Makefile help block; and explicit legacy references in `README.md`, `docs/operator-guide/scheduler.md`, `docs/operator-guide/workflows.md`, and `docs/reference/outputs.md`.
  - Confirmed the modern surface that must remain is: `bash scripts/run_stories_refresh.sh`, `bash scripts/run_explorer_refresh.sh`, `make stories-refresh-once`, `make explorer-refresh-once`, and `make full-refresh-once`, plus the documented per-job `stories_*` / `explorer_*` lock-log-state files.
  - Verification plan for implementation is: legacy-reference grep, modern-surface grep, then `make help`, `make test`, and `make docs-build`.
- Architect notes:
- Implementer notes:
  - Deleted `scripts/run_scheduled.sh`.
  - Removed legacy scheduler wiring from `Makefile`: `SCHEDULER_SCRIPT`, `.PHONY` entries, legacy help block, and targets `scheduler-dry-run`, `scheduler-once`, `status`, `tail-log`.
  - Cleaned only the named docs surface: `README.md`, `docs/operator-guide/scheduler.md`, `docs/operator-guide/workflows.md`, and `docs/reference/outputs.md`.
  - Kept the modern Stories / Explorer refresh entrypoints and `stories_*` / `explorer_*` lock-log-state docs intact.
- Verification:
  - Scoped legacy-reference grep across target files/scripts: clean.
  - Modern-surface grep across target files/scripts: ok.
  - `make help`: passed.
  - `make test`: failed during API test collection with `ModuleNotFoundError: No module named 'fastapi'` and `ModuleNotFoundError: No module named 'fastapi.datastructures'`.
  - `make docs-build`: passed.
- Result:
  - Bounded legacy scheduler family removed cleanly.
  - Modern refresh/orchestration surface preserved.
  - Verification is green except for the pre-existing/unrelated FastAPI environment issue in `make test`.