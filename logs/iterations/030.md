# iter/030

Planning started for repo documentation cleanup, English normalization, and active-tree markdown hygiene.

## Status
- inspected repo root, `docs/`, `logs/iterations/`, `artifacts/`, and runtime/generated clutter surfaces
- confirmed canonical docs surface is `README.md` + `docs/`
- classified tracked root docs into: canonical keep, preserve-but-move, and workflow/process clutter
- identified untracked `PLAN.md` / `PROJECT_BRIEF.md` / `TASK_CONTRACT.md` / `TODO.md` as agent scaffolding that should not survive as active repo docs
- identified mixed-language docs needing English normalization only if retained after cleanup
- wrote iter/030 planning artifacts and set `STATUS.md` to `PLANNING_DONE`

Planner note: the repo does not need a bonfire. It needs root-level markdown discipline — keep the real history, move it somewhere sane, and stop treating agent scratch files like project documentation.

- Implemented the approved cleanup pass: moved tracked root audits/specs/reviews into `docs/historical/`, archived prior process artifacts under `docs/historical/process/`, deleted stray `info.txt`, and relocated iter/030 agent scaffolding under `.agent/iter-030/`.
- Updated `README.md`, `docs/index.md`, `docs/historical/index.md`, and `mkdocs.yml` so the canonical docs surface is explicit and the moved historical material stays discoverable.
- Translated the retained archived iter/029 results summary to English instead of wasting time translating junk that left the active tree.
- Verification note: `make docs-build` could not run here because repo `preflight` failed with `uv missing`; direct `python3 -m mkdocs build --strict` also failed because `mkdocs` is not installed in this container.
