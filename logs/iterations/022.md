# Iteration 022

- Date (UTC): 2026-03-26 22:35
- Base branch: iter/021
- Working branch: iter/022
- Goal: verify and reclassify the remaining ambiguous legacy/manual surfaces without speculative cleanup
- Planner notes: wrote `PLAN.md` with hard classification criteria (`keep`, `manual-but-supported`, `legacy-but-retained`, `safe demote`, `safe remove later`, `defer`), target-specific evidence checks for `rss_discovery.py` / strategy barrel residue, story-matching helper scripts, and the legacy scheduler surface, plus tight limits on any implementer follow-up edits
- Architect notes:
- Implementer notes:
  - verified `RSSDiscoveryStrategy` has no in-repo consumers; only the module itself plus the package re-export mention it, while active `rss_discovery` hits elsewhere are metrics/name labels from layered discovery and adapter tests
  - verified the story-matching helper scripts are coherent manual tooling, not random leftovers: each exposes a real CLI, uses live support code in `src.analysis.story_eval` / `src.analysis.story_review`, and is documented end-to-end in `docs/architecture/story-matching-eval.md`
  - verified `scripts/run_scheduled.sh` is still runnable and intentionally legacy; the actual canonical recurring surface is `run_stories_refresh.sh` + `run_explorer_refresh.sh` / `make full-refresh-once`
  - applied one tiny truth-alignment change in `Makefile`: `make help` now separates canonical refresh commands from `Legacy scheduler helpers (retained, not canonical)`
- Verification:
  - repo-wide reference checks for `RSSDiscoveryStrategy` / `rss_discovery`
  - direct inspection of target scripts, `Makefile`, README, and operator/docs surfaces
  - `make help`
- Result:
  - `src/core/strategies/rss_discovery.py` + `src/core/strategies/__init__.py` classified `safe remove later`
  - story-matching / review helper scripts classified `manual-but-supported`
  - `scripts/run_scheduled.sh` classified `legacy-but-retained`
  - legacy scheduler Makefile help surface classified `safe demote` and demoted accordingly