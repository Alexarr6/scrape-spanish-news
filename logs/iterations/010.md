# Iteration 010

- Date (UTC): 2026-03-26 16:12
- Base branch: main
- Working branch: iter/010
- Goal: Trace and fix where same-story membership is lost between Explorer and Stories.
- Planner notes:
  - Inspected the actual hot path: clustering/persistence in `src/analysis/pipeline.py`, Stories read-side in `src/analysis/readside.py`, and Explorer behavior in `src/semantic/dbstore.py` / `src/api/v1/semantic.py`.
  - Key finding: Stories is a direct read of persisted `cluster_members`, while Explorer can surface broader embedding-space neighbors through `nearest_neighbors(...)`. So the likely bug is upstream membership loss before persistence, not a Stories query trimming members.
  - Wrote an executable PLAN.md with a strict checkpoint trace: candidate pairs -> accepted edges -> raw component -> guarded closure -> `cluster_members` -> Stories API -> Explorer neighbor/membership payload.
  - Ranked hypotheses and bounded post-diagnosis fix classes so the implementer does not do dumb random threshold fiddling.
- Architect notes:
- Implementer notes:
  - Tried to interrogate the local Postgres backing store from `.env`, but `127.0.0.1:5433` was down (`connection refused`), so there was no honest way to pull a live `story_cluster_id`/`cluster_members` tuple from this machine.
  - Switched to a mechanical trace of the responsible pipeline stage instead of faking DB evidence.
  - Reproduced the exact count-loss pattern inside guarded closure: a coherent medium-only raw component of five accepted articles (`1-2-3-4-5`) produced `raw=[[1,2,3,4,5]]` but, before the fix, guarded closure shrank that to `[[1,2],[3],[4],[5]]`.
  - Root cause: `_audit_medium_component(...)` hard-rejected medium-only raw components larger than 3, so closure skipped preservation and the later singleton attach could only rebuild the strongest seed pair when the remaining supports lived around `0.55`.
  - Shipped the smallest direct fix in `src/analysis/pipeline.py`: allow coherent medium-only component preservation up to size 5, with slightly stricter default-mode thresholds for size 4-5 (`mean >= 0.55`, `best >= 0.56`) while keeping the same risky-bridge / penalty / secondary-form guardrails.
  - Added regression coverage in `tests/test_story_clustering.py::test_guarded_components_preserve_coherent_medium_only_chain_of_five` to pin the `5 -> 2` loss stage and prove the repaired result is `[[1,2,3,4,5]]`.
- Verification:
  - `/home/node/.local/bin/uv run pytest tests/test_story_clustering.py tests/test_story_pair_scoring.py tests/test_story_matching_eval.py tests/test_story_candidate_generation.py tests/test_story_review.py`
  - Result: `40 passed`
- Result:
  - Implementation complete. The true bottleneck was guarded closure’s size-3 cap for clean medium-only raw components, not Stories read-side. The bounded fix is shipped and regression-protected.