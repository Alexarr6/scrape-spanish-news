# Iteration 025

- Date (UTC): 2026-03-26 23:06
- Base branch: iter/024
- Working branch: iter/025
- Goal: plan and implement phase 1 of the frontend navigation / URL-state refactor
- Planner notes: inspected `App.tsx`, both URL-state hooks, and `navigation.ts`; bounded phase 1 to explicit app-mode ownership, shared URL primitives, and deliberate Stories/Explorer handoff rules; wrote invariants for mode switching and deep-link survival; explicitly deferred any router rewrite or broad param redesign.
- Architect notes:
  - Re-inspected the live frontend seam and confirmed the real ownership bug: `App.tsx` resolves mode from ambient URL state in two places while nav items are frozen at module scope.
  - Approved a tiny app-mode helper layer so `App.tsx` becomes the single owner of top-level surface mode, active nav state, and page selection.
  - Approved one shared URL-mechanics layer for search-param read/delete/write and generic integer parsing, while explicitly keeping Stories and Explorer as two separate URL-state domains.
  - Approved `navigation.ts` as an explicit cross-surface handoff file only: Stories builder drops semantic mode; Explorer builder sets `view=semantic`, preserves the seeded transition contract, and clears only intentionally incompatible Explorer params.
  - Wrote the implementation-ready architecture handoff in `docs/architecture/2026-03-26-navigation-url-state-phase1-architecture.md`.
- Implementer notes:
  - Added `frontend/src/lib/urlState.ts` to hold only shared app-mode and URL search mechanics.
  - Refactored `frontend/src/App.tsx` so one `appMode` decision now drives both navigation active state and rendered surface.
  - Refactored `useClusterUrlState.ts` and `useExplorerUrlState.ts` to share generic parsing/serialization helpers while keeping Stories and Explorer query ownership separate.
  - Reworked `frontend/src/lib/navigation.ts` into explicit handoff builders (`buildStoriesSurfaceHref`, `buildExplorerSurfaceHref`) and updated Stories-side call sites in `StoryFocusPanel.tsx`.
  - Preserved seeded Stories → Explorer transitions, including `sem_story_cluster` / `sem_article` and the visual defaults `sem_mode=highlight` + `sem_color=active-match`.
- Verification:
  - `cd frontend && npm run build` ✅
  - Build produced non-blocking Vite warnings about large chunks and a `@loaders.gl/worker-utils` `spawn` warning, but exited 0.
- Result: DONE
