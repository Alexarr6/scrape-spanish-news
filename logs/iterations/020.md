# Iteration 020

- Date (UTC): 2026-03-26 21:43
- Base branch: iter/019
- Working branch: iter/020
- Goal: Align the bounded docs/operator contract with the surviving canonical scheduler surface and output/state layout.
- Planner notes:
  - Reconfirmed the exact drift to fix: `docs/operator-guide/workflows.md` still treated `bash scripts/run_scheduled.sh` as the supported scheduler entrypoint, while `README.md`, `docs/operator-guide/scheduler.md`, and `Makefile` treated Stories + Explorer split wrappers plus `make full-refresh-once` as the active operator surface.
  - Reconfirmed `docs/reference/outputs.md` was still legacy-only on scheduler state/log docs and needed alignment to the current per-job `stories_*` / `explorer_*` files while keeping legacy scheduler wording explicit where that path still exists.
  - Bounded the implementation surface to `docs/operator-guide/workflows.md` and `docs/reference/outputs.md`; `docs/operator-guide/scheduler.md` and `README.md` stayed untouched.
- Architect notes:
- Implementer notes:
  - Rewrote the workflow scheduler section so the main recurring path now points at `run_stories_refresh.sh`, `run_explorer_refresh.sh`, and `make full-refresh-once`.
  - Added an explicit legacy note for `run_scheduled.sh` so it remains documented as runnable scrape-only infrastructure, not as the main product scheduler path.
  - Updated outputs/state docs to reflect the active per-job lock/log/state files and preserved the legacy scheduler file family as a clearly marked legacy layout.
- Verification:
  - Ran `make docs-build`
  - Result: passed (exit 0)
  - Note: MkDocs still reports pre-existing non-blocking pages outside `nav`; no new docs build failures were introduced.
- Result:
  - Docs/operator contract aligned for lot 4B within the approved bounded scope.
