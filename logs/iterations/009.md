# Iteration 009

- Date (UTC): 2026-03-24 22:08
- Base branch: iter/008
- Goal: plan the next bounded Explorer iteration for **bias lens only**
- Planner notes:
  - re-read required planning files: `PROJECT_BRIEF.md`, `TASK_CONTRACT.md`, `PLAN.md`, `STATUS.md`, `RESULTS.md`, `logs/iterations/008.md`, `docs/architecture/2026-03-24-explorer-editorial-lenses-architecture.md`, and `docs/reviews/2026-03-24-iter-008-explorer-article-type-lens-review.md`
  - inspected actual repo state for Explorer/editorial wiring in:
    - `src/api/contracts/semantic.py`
    - `src/api/v1/semantic.py`
    - `src/semantic/dbstore.py`
    - `src/analysis/readside.py`
    - `frontend/src/lib/types.ts`
    - `frontend/src/lib/explorerEditorial.ts`
    - `frontend/src/hooks/useExplorerUrlState.ts`
    - `frontend/src/routes/ExplorerPage.tsx`
    - `frontend/src/components/explorer/ExplorerControlBar.tsx`
    - `frontend/src/components/explorer/ExplorerContextRail.tsx`
    - `frontend/src/components/explorer/MapPanel.tsx`
    - `tests/test_api_semantic_explorer.py`
  - confirmed current Explorer article-type slice is real and stable, but bias is still blocked by contract gaps rather than by missing source data
  - confirmed Explorer SQL already selects `bias_label` and `bias_confidence`, and repo review-flag logic already computes the caution states bias needs (`low_confidence`, `unclear_bias`, `pending_analysis`, `failed_analysis`, `out_of_domain`, `needs_review`)
  - confirmed the current API contract drops bias from `ExplorerPoint.editorial_preview`, Explorer metadata exposes only `article_type`, backend filter logic only supports `article_type`, and frontend Explorer types/controls/color modes are hardcoded to article type
  - defined the next iteration as **bias-only** and explicitly rejected:
    - tone in the same pass
    - a generic multi-lens builder
    - confidence-slider theatrics
  - locked recommended bias semantics:
    - highlight = keep full cloud, emphasize only strict confident matches
    - filter = narrow to strict confident matches only
    - color-by-bias = distribution view for confident in-domain labels; low-confidence / unknown / pending / failed / out-of-domain stay muted diagnostic states
  - explicit handling rules captured in `PLAN.md` for:
    - unknown / unclear bias
    - low confidence (`bias_confidence < 0.45`)
    - pending
    - failed
    - limited applicability
    - out_of_domain
  - identified exact missing contracts:
    - add `bias_label` + `bias_confidence` to `ExplorerPointEditorialPreview`
    - add `bias_label` metadata/counts to Explorer meta/filters
    - add strict backend filter support for `sem_editorial_dim=bias_label`
    - widen frontend types from article-type-only to bounded `article_type | bias_label`
    - add frontend bias helpers, bias color mode, and bias legend/copy semantics
- Readiness verdict:
  - bias is **conditionally ready** for a bounded Explorer slice
  - not ready for shipping without point-preview + metadata + strict-confidence semantics
- Smallest shippable slice:
  - add point-level bias preview fields
  - add bias metadata/options/counts
  - add one bias lens target at a time with highlight/filter/color semantics
  - keep low-confidence / unknown / pending / failed / out_of_domain out of positive-match behavior
- Recommended next agent:
  - **architect** first
  - then backend/data implementer
  - then frontend implementer
  - then architect review
- Result:
  - updated `PLAN.md` with the bounded bias-only plan for the next Explorer iteration
  - wrote architecture handoff doc: `docs/architecture/2026-03-24-explorer-bias-lens-architecture.md`
  - updated this iteration log instead of mutating iter/008 history

## architect follow-up completed
- inspected actual repo contracts/helpers to lock the bias slice against real code rather than planner guesses:
  - `src/api/contracts/semantic.py`
  - `src/api/v1/semantic.py`
  - `src/semantic/dbstore.py`
  - `src/analysis/readside.py`
  - `frontend/src/lib/types.ts`
  - `frontend/src/lib/explorerEditorial.ts`
  - `frontend/src/components/explorer/MapPanel.tsx`
  - `tests/test_api_semantic_explorer.py`
- confirmed the key mismatch precisely:
  - Explorer SQL/read-side already has `bias_label` + `bias_confidence`
  - `ExplorerPointEditorialPreview` still drops both fields
  - `_build_explorer_where_clause()` still only supports `article_type` filter semantics
  - Explorer metadata still exposes only `article_type`
  - frontend active editorial target / matching / color mode are still article-type-only
- locked strict bias-positive semantics for v1:
  - positive match requires `analysis_status=completed`
  - `editorial_applicability=full`
  - exact `bias_label` match
  - not `low_confidence`
  - not `unclear_bias`
  - not `out_of_domain`
- locked exact handling for non-match diagnostic states:
  - `low_confidence`: visible in highlight, excluded from filter, muted in color-by-bias
  - `unknown/unclear`: visible in highlight, excluded from filter, muted in color-by-bias
  - `pending`: visible in highlight, excluded from filter, muted in color-by-bias
  - `failed`: visible in highlight, excluded from filter, muted in color-by-bias
  - `limited`: visible in highlight, excluded from filter, muted in color-by-bias
  - `out_of_domain`: never a match, excluded from filter, muted in color-by-bias
- specified exact backend additions:
  - add `bias_label` + `bias_confidence` to `ExplorerPointEditorialPreview`
  - allow `sem_editorial_dim=bias_label`
  - implement strict bias filter SQL semantics instead of raw label equality
  - add `bias_label` options plus bias diagnostic coverage counts to Explorer metadata
- specified exact frontend additions:
  - widen `ExplorerEditorialDimension` to `'article_type' | 'bias_label'`
  - add `sem_color='bias'`
  - add bias-specific helper functions / palette / bucket logic
  - support one active bias target at a time in the toolbar
  - add explicit rail/legend copy for confident matches vs diagnostic states
- kept the slice bounded:
  - no tone
  - no multi-lens builder
  - no confidence sliders
  - no broad Explorer redesign
- recommended next execution order from this point:
  - backend/data implementer
  - frontend implementer
  - architect review

## architect review completed
- reviewed the landed Explorer bias-lens slice across planning docs, backend contracts/API/tests, and frontend Explorer wiring
- verification rerun during review passed:
  - `/home/node/.local/bin/uv run --group dev python -m pytest tests/test_api_semantic_explorer.py` → `14 passed`
  - `cd frontend && npm run build` → passed
- verdict: **approve with caveats**
- strengths:
  - interaction model stayed coherent: one active editorial target, highlight/filter/color semantics remain legible
  - strict bias semantics stayed honest: only confident full-applicability completed matches count as positives
  - diagnostic states are handled explicitly and kept muted instead of masquerading as ideological categories
  - article-type behavior remained intact after widening
- caveats:
  - metadata `coverage` is still a flat mixed namespace and will turn into sludge if more dimensions get piled on casually
  - backend filter semantics and frontend strict-match semantics are still intentionally duplicated; safe now, risky if tone/generic builders arrive too soon
- next bounded recommendation:
  - do one contract-hardening pass before adding any new editorial dimension
  - specifically: tighten/structure editorial metadata and add focused frontend interaction tests for bias highlight/filter/color behavior
- explicitly avoid next:
  - tone
  - generic multi-lens builder
  - AND/OR editorial logic
  - confidence sliders
