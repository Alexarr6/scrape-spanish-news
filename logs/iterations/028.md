# iter/028

Planning started for analysis pipeline structural refactor phase 1.

## Intent
- inspect `src/analysis/pipeline.py`
- select one bounded seam
- implement with planner -> implementer chain

## Status
- branch created: `iter/028`
- brief and task contract written
- planner handoff completed

## Planner note
- Chosen phase-1 seam: extract guarded cluster closure/component assembly from `src/analysis/pipeline.py` into a dedicated module (proposed `src/analysis/story_closure.py`).
- Why this seam: it is the largest coherent responsibility block with strong existing test coverage, and it reduces god-file pressure without changing scoring, candidate generation, or persistence behavior.
- Scope guard: move closure helpers only; keep `ClusterPipeline.build_clusters()`, `score_pair()`, candidate generation, thresholds, heuristics, and persistence contracts unchanged.

## Implementation notes
- Added `src/analysis/story_closure.py` with `StoryClosureBuilder` and moved the approved guarded-closure/component-assembly helpers into it.
- Kept `ClusterPipeline` public behavior intact and preserved old private method names as thin delegation wrappers because clustering tests monkeypatch/assert against them.
- Passed `high_recall_mode` explicitly into the closure builder instead of letting the extracted logic reach back into pipeline internals.
- Verification: `uv run python -m pytest tests/test_story_clustering.py tests/test_story_pair_scoring.py tests/test_story_candidate_generation.py tests/test_story_matching_eval.py tests/test_story_review.py` → `40 passed`.
